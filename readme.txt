30.10.2023
Я савсем запутался в логике программы, по этому вёрсткой почти не занимался. К тому же не савсем понял, что должно в финальном результате получиться.
..........
Что сейчас происходит на странице:
При первом рендере полностью выводится только первая статья которая указывается индексом в массиве данных.
Статья выводится по шаблону Vue.component('my-article-detail')

Так как я не понял какой требуется результат и какие типовые данные ждать то принял такой тестовый формат
У объекта есть титул, дата, банер, и контент

Контент представляет из себя последовательность праграфов разделённых /n при этом ссылки на картинки отмечены ключевой строкой "IMAGE:" которая обрабатывается при создании отображения
??? Как сразу создать фильтрованый массив и отобразить его, не придумал. ???
Теги вытаскиваются перобором всех объектов, фильтруются повторы.
При клике на тег отрабатывает метод filterByTag(tag) который создаёт фильтрованый массив из которого по немного другому шаблону из тогоже файла подставляются статьи (по моей задумке только заголовок, дата и кртинка).
До этого момента программа не вызывает особых проблем (кроме первого рендера без учёта фильтра).
..........
По какойто причине переменная selectedTag в vue.devtools всегда пустая, но в консоль тестовый вызов this.sectedTag при фильтрации отображает корректно. В более ранней версии она работала нормально, но пока эксперементировал с вызовом сортировки както так получилось. При этом нажатия на теги отрабатываются нормально.
Заголовки статей сделаны ввиде ссылок, событие клика пробрасывается из компонента. По моей задумке выбраная статья должна была отобразиться полностью. Замена происходит нормально, но не удалось вызвать сортировку по тегам заново, чтобы убрать её "превью" из списка.

--------------

02.11.2023
Рефакторинг на CLL + vue 2 + SCSS

В целом удалось полностью восстановить состояние сайта на момент предыдущей фиксации.
Были решены многие предыдущие вопросы по логике программы.
Улучшена связь страниц. Теперь переходами управляет библиотека VueRouter. Шапка неизменна при переходах. 
На странице Blog кнопки статей активны. Переносят на страницу /blog/details.
С переходом отправляется индекс кликнутой статьи и она отображается развёрнутая.
Вёрстка /blog/details всё ещё отсутствует.

Проблема одна. Не могу разобраться в файловых путях. <img> вроде разобрался как работают, но фоновые изображения не удаётся установить.
Баннер на странице /blog/details изначально был стилизован как фон для блока.